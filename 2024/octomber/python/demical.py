# Create a program which receives a binary number and converts it to decimal

def convert_to_demical(binary_number):
    for i in range(binary_number):
        return binary_number + (binary_number * 0) + (binary_number * 1) 

print(convert_to_demical(11011))